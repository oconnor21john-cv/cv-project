# Set VITE_API_BASE_URL in the Netlify dashboard (Site → Environment variables).
# Example: VITE_API_BASE_URL=https://your-order-service.onrender.com
# Vite bakes this value in at build time; without it the UI falls back to localhost.
# VITE_API_BASE_URL=https://your-order-service.onrender.com

